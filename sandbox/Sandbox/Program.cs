using System;

class Program
{
    static void Main(string[] args)
    {
        Console.WriteLine("Hello Sandbox World!");

        //namespace > classes > members (method) > statements

        //method always uses parenthesis
    }
}

//getters and settrs = add security to fields by encapsulation->accessors found within properties
//properties = combine aspects of both fields and methods->share name with a field
//get = used to return property value
//set = used to assign a new value
//value keyword = defines the value being assigned by the set parameter